---
name: flat-orphan
description: Placed directly under skills/ as a flat .md. The runtime requires <dir>/SKILL.md, so this is never discovered.
---

# flat-orphan

The declaration is valid. The location is inside the search path. Only the shape is wrong.
That combination is the symptom: "placed where it would be found, in a form that is not found".
