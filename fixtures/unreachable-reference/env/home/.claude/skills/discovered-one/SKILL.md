---
name: discovered-one
description: Contrast case. Lives in <dir>/SKILL.md form, so the runtime discovers it.
---

# discovered-one

This skill exists only to prove the fixture is not blind: a correctly placed skill must
produce `discovered: true` and must never appear in findings.
